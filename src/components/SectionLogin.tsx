import React from 'react';
import { MyLogin } from '../UI/index';

const SectionLogin = () => {
    return (
        <section className='flex flex-col items-center'>
            <MyLogin/>
        </section>
    );
};

export {SectionLogin};